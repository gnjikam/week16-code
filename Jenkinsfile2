pipeline{
    agent any
    tools{
      maven 'M2_HOME'  
    }
    stages{
      stage('sonarqube scanner'){
        steps{
          sh 'sonar:sonar'
        }
      }
        stage('maven commands'){
            steps{
              sh 'mvn clean compile test install package'  
            } 
        }
    }
}